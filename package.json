{
  "name": "cityjson-toolbox-mcp",
  "version": "0.1.0",
  "description": "MCP server for manipulating, validating, converting, querying and documenting CityJSON datasets.",
  "type": "module",
  "bin": {
    "cityjson-toolbox-mcp": "./src/index.mjs"
  },
  "scripts": {
    "start": "node src/index.mjs",
    "check": "node --check src/index.mjs && node scripts/check-all.mjs",
    "test": "node --test",
    "render:diagrams": "python3 scripts/render_diagrams.py",
    "doctor": "node scripts/doctor.mjs",
    "docker:build": "docker build -f docker/Dockerfile -t cityjson-toolbox-mcp .",
    "docker:cache:val3dity": "docker build -f docker/Dockerfile --target val3dity-builder -t cityjson-toolbox-val3dity-builder .",
    "docker:cache:cjval": "docker build -f docker/Dockerfile --target cjval-builder -t cityjson-toolbox-cjval-builder .",
    "docker:doctor": "docker run --rm --entrypoint node cityjson-toolbox-mcp /app/scripts/doctor.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.0.0"
  },
  "keywords": [
    "mcp",
    "cityjson",
    "citygml",
    "cjio",
    "cjval",
    "val3dity",
    "cjdb",
    "3d-city-models"
  ],
  "license": "MIT"
}
